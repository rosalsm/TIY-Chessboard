{
  "name": "chessboard",
  "version": "0.0.0",
  "description": "chessboard",
  "main": "index.html",
 "scripts": {
   "test": "browser-sync start --server --startPath='tests.html' --files='js/*.js'",
   "start": "browser-sync start --server --startPath='index.html' --files='index.html,js/*.js,css/*.css'",
   "sass": "node-sass --output css/ scss/main.scss",
   "sass:watch": "npm run sass -- --watch"
  },
  "author": "",
  "license": "ISC",

  "devDependencies": {
    "browser-sync": "^2.9.6",
    "chai": "^3.3.0",
    "mocha": "^2.3.3",
    "node-sass": "^3.3.3"
  }
}
